class Player {
  constructor(position, groups) {
    this.position = position;
    this.groups = groups;
    this.image = document.getElementById(`img-player`)
  }

  update(dt) {
  }

  draw(ctx) {
  }
}
